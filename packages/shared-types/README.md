# @desaignsync/shared-types

Contratos versionados compartidos por la extensión, el Local Host y los
motores core (protocolo loopback, MCP, evidencia, findings, config, errores).
Sin dependencias de runtime; consumir siempre desde aquí, no por copia.

Ver `docs/backlog/DS-001.md`.
