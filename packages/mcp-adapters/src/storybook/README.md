# packages/mcp-adapters/src/storybook/README.md

Storybook preset/mapping built on top of the generic Design System adapter. Storybook MCP is
documented as a preview feature with changing APIs, so the product core never requires a Storybook
URL and this folder stays thin: mapping plus preset defaults only.

- **Owner:** Agent 3 - MCP Integrations
- **Waves:** Wave 2 (DS-007)
- **Status:** placeholder - no implementation yet (Wave 1 only establishes the skeleton).

Sources of truth: `docs/AI_Design_System_Compliance_MCP_Spec_v2.1.md (sections 7.2 and 11)` and `docs/backlog/DS-007.md`.
